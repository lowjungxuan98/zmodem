name: zmodem
description: A state machine based, bring-your-own-io style implementation of the ZMODEM protocol in pure Dart
version: 0.0.7
repository: https://github.com/lowjungxuan98/zmodem

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  async: ^2.13.0
  convert: ^3.1.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.30.0
